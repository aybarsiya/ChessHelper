from Enums import *

class Chessboard:
        initialized = bool(0)
        
        
        
        def __init__(self):
                
                                
                
                self.initialized = bool(1)
                
                
class Square:
        posX = " "
        posY = int(0)
        piece = Piece.EMPTY.value
        colour = int(-1)